name: stockfacil
description: App para gestión de inventario
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.4.0
  cloud_firestore: ^4.3.0

flutter:
  uses-material-design: true